# Demo Merchant - Environment Configuration

# Server
PORT=3002

# Merchant Identity
MERCHANT_NAME=Premium Data Corp
MERCHANT_ADDRESS=0x...  # Sui address for receiving payments
MERCHANT_PRIVATE_KEY=suiprivkey1...  # Ed25519 private key for JWT signing

# Payment Configuration
RESOURCE_PRICE=10000000  # 10.00 USDC (6 decimals)

# Facilitator (from facilitator/.env)
FACILITATOR_ADDRESS=0x...  # Facilitator's Sui address
FACILITATOR_FEE=500000  # 0.50 USDC (6 decimals)

# SUI Network
SUI_NETWORK=localnet  # Options: localnet, testnet

# Coin Type (optional - auto-detected from SUI_NETWORK if not set)
# COIN_TYPE=0x34f1b450...::mock_usdc::MOCK_USDC
# Leave unset to auto-detect: localnet=MockUSDC, testnet=real USDC

# Invoice Settings
INVOICE_EXPIRY_SECONDS=3600  # How long invoices are valid (1 hour)
